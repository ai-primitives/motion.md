import { renderMedia } from "@remotion/renderer"

export const render = async (markdownFile: string) => {
  // TODO: Implement video generation from markdown
  console.log(`Rendering ${markdownFile}...`)
}
